# Anthropic API (populated in Week 3 when the AI layer is wired)
ANTHROPIC_API_KEY=

# OneDrive sync path — folder where Power Automate drops CSVs
ONEDRIVE_DATA_PATH=

# Local SQLite database path (relative to project root)
SQLITE_PATH=data/cashflow.db

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO